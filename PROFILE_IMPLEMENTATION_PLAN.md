# プロファイル機能・公式アプリ互換 実装計画

## 目的

1. 起動直後の画面でプロファイルを選択できるようにする。
2. BIGBIG WON公式アプリとプロファイルを共有する。
3. 保存先などをユーザーが設定しなくても、自動的に双方向で共有されるようにする。

## 結論

Lite専用DBと公式DBを同期する方式ではなく、公式アプリの`Config.db`を両アプリの共通ストアとして直接利用する。

これにより、次の問題を避けられる。

- 保存先の手動設定
- 二重管理されたプロファイルのマージ
- 同期方向の判定
- 同名プロファイルの重複生成
- Liteと公式アプリ間の同期漏れ

## 調査で確認できた事実

### 公式アプリの保存先

```text
C:\ProgramData\GamepadAssistant\Config.db
```

`Config.db`はSQLiteデータベースで、WALモードが使用されている。

### プロファイルテーブル

公式アプリは`t_Config`テーブルを使用する。

主要な列は次のとおり。

| 列 | 用途 |
| --- | --- |
| `FID` | ローカル行ID |
| `FID_WebService` | 公式クラウド側ID。ローカルプロファイルは`-1` |
| `FPhoneUUID_WebService` | 公式アプリ側の端末識別情報 |
| `FDevUUID` | コントローラーUUID |
| `FDevName` | コントローラー名 |
| `FUserID_WebService` | 公式クラウド側ユーザーID。ローカルは`-1` |
| `FConfigName` | プロファイル名 |
| `FConfigJson` | プロファイル本体 |
| `FFirmwareVersion` | ファームウェア情報 |
| `FZKMVersion` | ZKMバージョン |
| `FDateTimeCreate` | 作成日時 |
| `FDeleted` | 論理削除フラグ |

### プロファイル本体

現在保存されている`FConfigJson`は、484個の整数からなるJSON配列である。

これはLiteがすでに読み書きできるv37の484バイトプロファイルと同じ形式であり、別形式への変換は不要。

公式アプリの削除処理は物理削除ではなく、次の論理削除になっている。

```sql
UPDATE t_Config SET FDeleted = 1 WHERE FID = ?;
```

### 現在のLite側の制約

- 起動時にコントローラーを検索し、そのままD6プロファイル読み取りを実行している。
- プロファイルキャッシュがHIDデバイスパスと結び付いている。
- コントローラー未接続時はファイルプロファイルも読み込めない。
- 設定の「保存」がDB保存ではなく、実機へのD7書き込みになっている。
- 永続的なプロファイル一覧はまだ存在しない。

## 実装方針

### 1. 公式互換プロファイルストア

`src-tauri/src/profiles.rs`を追加し、SQLite操作を集約する。

#### 保存先の解決

1. Windowsの`ProgramData`を取得する。
2. `GamepadAssistant\Config.db`を自動検出する。
3. DBが存在しない場合は、公式互換スキーマで自動作成する。
4. 保存先を選択する設定画面は作らない。

#### 追加する依存関係

- `rusqlite`
- `serde_json`

SQLiteはアプリ内で確実に利用できる構成にする。

#### Tauriコマンド

コマンド数を増やしすぎず、次の単位にまとめる。

```text
list_profiles
load_saved_profile
save_profile
delete_profile
```

`save_profile`は新規作成と更新の両方を担当する。複製は既存プロファイルを読み込み、IDなしで保存することで実現する。

#### DB書き込み規則

- SQL文字列連結を行わず、必ずプリペアドステートメントを使う。
- 書き込みはトランザクション内で行う。
- 既存行を更新する場合、クラウド関連IDなど未知のメタデータを保持する。
- 新しいローカルプロファイルは`FID_WebService = -1`、`FUserID_WebService = -1`とする。
- 削除は`FDeleted = 1`の論理削除とする。
- 一覧取得は`FDeleted = 0`だけを対象とする。
- DBの必須列を起動時に検査する。
- 未知の新しいスキーマだった場合は書き込みを止め、読み取り専用として扱う。

### 2. プロファイルと実機状態の分離

現在の`CachedProfile`を、次の二つの責務へ分離する。

#### ProfileDocument

- DB上のID
- プロファイル名
- 機種・UUIDなどのメタデータ
- 484バイトの生プロファイル
- CRC
- スティック、キーバインド、連射、振動の編集状態
- DBから読み込んだ時点の内容

#### DeviceSession

- HIDデバイスパス
- 製品名
- コントローラーUUID
- ZKMバージョン
- 接続状態

これにより、コントローラーが接続されていなくてもプロファイルを開いて編集できるようにする。

### 3. コントローラー識別

起動時のスキャンでは長いD6読み取りを行わず、次の短い読み取りだけを行う。

| 情報 | コマンド |
| --- | --- |
| デバイスUUID | `EF` |
| ZKMバージョン | `0B` |

取得したUUIDを`t_Config.FDevUUID`と照合し、接続中のコントローラー向けプロファイルを優先表示する。

D6による本体プロファイル読み取りは「実機から読み込む」を押した場合だけ実行する。自動リトライは行わない。

### 4. 編集と実機書き込みの分離

現在の設定保存処理は、編集とD7書き込みが一体化しているため分離する。

#### プロファイルを保存

1. UIの編集値を484バイトの生プロファイルへ反映する。
2. 未知バイトをそのまま保持する。
3. CRCを再計算する。
4. `Config.db`へ保存する。
5. 実機へは何も送信しない。

#### 実機へ適用

1. 保存済みの484バイトプロファイルを使用する。
2. 接続中のデバイスが選択時と同じであることを確認する。
3. D7の9フラグメントを書き込む。
4. `A5 05 D7` ACKを検証する。
5. 自動D6再読込は行わない。

「実機へ適用」は未保存の編集がある場合には無効化する。先にプロファイル保存を要求する。

### 5. 起動画面をプロファイル選択画面へ変更

現在のホーム画面をプロファイルライブラリに置き換える。

#### 表示内容

- 接続中コントローラー
- 接続中UUIDと一致するプロファイル
- その他の共有プロファイル
- プロファイル名
- コントローラー名
- 作成日時
- 対応プロファイルバージョン
- 互換・非互換状態

#### 操作

- プロファイルを開く
- 複製
- 名前変更
- 論理削除
- ファイルから読み込む
- 実機から読み込んで新規作成

未知バイトを正しく作れないため、完全な空プロファイルは生成しない。新しいプロファイルは次のいずれかを元に作成する。

- 接続中コントローラー
- 既存プロファイルの複製
- インポートした公式形式ファイル

### 6. 保存操作の整理

プロファイル編集画面の操作を次のように整理する。

| 操作 | 保存先 |
| --- | --- |
| プロファイルを保存 | 公式共有`Config.db` |
| 実機へ適用 | コントローラーのD7プロファイル |
| デバイス設定を保存 | ポーリングレート・ステップ精度の実機コマンド |
| マクロを保存 | 実機のD8マクロスロット |

スティック、キーバインド、連射、振動は一つの「プロファイルを保存」でまとめて保存する。

### 7. 自動共有と再読込

別DBとの同期処理は作らない。公式DBを直接使用するため、保存した時点で共有済みになる。

Lite側では次のタイミングで一覧を自動再読込する。

- 起動時
- プロファイル画面へ戻ったとき
- ウィンドウが再フォーカスされたとき
- 作成、更新、改名、削除の直後

常時ファイル監視は最初の実装では追加しない。ウィンドウフォーカス時の再読込で十分な自動共有を実現し、不要な監視処理を避ける。

公式アプリ側の一覧更新タイミングは変更できないため、Liteで作成したプロファイルは公式アプリの画面再表示または再起動後に表示される可能性がある。

### 8. 外部変更と同時書き込み

公式アプリとLiteを同時に使用できるよう、次の対策を入れる。

- SQLiteのbusy timeoutを設定する。
- 短い書き込みトランザクションを使用する。
- 編集開始時のDB内容を保持する。
- 保存直前に対象行が変更されていないか比較する。
- 外部変更があった場合は黙って上書きしない。
- 「公式側の内容を再読込」または「別名で保存」を選べるようにする。
- プロファイル名を行IDの代わりに使用しない。内部識別は常に`FID`を使う。

### 9. バックアップ

公式DBを直接変更するため、初回書き込み前にSQLiteのオンラインバックアップを自動作成する。

保存先の例：

```text
%LOCALAPPDATA%\com.bigbigwon.lite\backups\Config-YYYYMMDD-HHMMSS.db
```

WAL使用中のDBを単純なファイルコピーで複製せず、SQLiteのバックアップAPIを使用する。

## 対応範囲

初期実装で共有するのは、現在Liteが編集できるv37・484バイトのベースプロファイルとする。

### 対応する項目

- スティック設定
- 矩形アルゴリズム
- キーバインド
- M1～M4連射
- 連射速度
- 振動
- 未知バイトを含む生プロファイル全体

### 今回含めない項目

- `Macro.db`とのマクロ共有
- `Light.db`との照明設定共有
- 公式クラウドAPIとの直接通信
- ファームウェア更新
- v34、v35、v36、v39、v60の編集

484バイト以外の公式プロファイルは一覧に表示できるようにしても、編集と実機適用は無効化し、非対応理由を表示する。

## 変更予定ファイル

| ファイル | 変更内容 |
| --- | --- |
| `src-tauri/Cargo.toml` | SQLite・JSON依存関係の追加 |
| `src-tauri/src/profiles.rs` | 公式互換DBリポジトリを新規追加 |
| `src-tauri/src/lib.rs` | Tauriコマンド、アプリ状態の再構成 |
| `src-tauri/src/device.rs` | DeviceSession、EF/0B、明示的D6/D7処理 |
| `src-tauri/src/protocol.rs` | オフライン編集用の純粋なプロファイル更新処理 |
| `src/main.ts` | プロファイル一覧・選択・保存フロー |
| `index.html` | 起動直後のプロファイルライブラリUI |
| `src/styles.css` | プロファイルカードと状態表示 |
| `README.md` | 共有仕様、保存先、制限事項 |

## 実装順序

- [ ] `profiles.rs`と一時DBを使ったDBテストを作成
- [ ] 公式DBの自動検出とスキーマ検査を実装
- [ ] 一覧、読み込み、新規保存、更新、論理削除を実装
- [ ] プロファイル文書とデバイスセッションを分離
- [ ] 設定変更をD7送信しない純粋な編集処理へ変更
- [ ] EF/0Bによる自動デバイス識別を追加
- [ ] 起動画面をプロファイルライブラリへ変更
- [ ] 保存と実機適用のUIを分離
- [ ] ウィンドウフォーカス時の自動再読込を追加
- [ ] 外部変更競合とSQLiteロック処理を追加
- [ ] オンラインバックアップを追加
- [ ] 複製DBで統合テスト
- [ ] 使い捨てプロファイルで公式アプリとの双方向確認
- [ ] READMEとプロトコル資料を更新

## テスト計画

### DB単体テスト

- 公式スキーマを持つ空DBを作成できる。
- 既存`t_Config`を読み取れる。
- `FDeleted = 1`を一覧から除外する。
- 新規プロファイルを公式形式で保存できる。
- 更新時に未知のメタデータを保持する。
- 改名しても同じ`FID`を維持する。
- 削除が論理削除になる。
- 新しい未知のスキーマに書き込まない。

### プロファイル単体テスト

- `FConfigJson`を484バイトへ変換できる。
- 各値が`0..255`の範囲外なら拒否する。
- 長さヘッダーが484でなければ拒否する。
- CRC不一致を拒否する。
- 編集後にCRCが更新される。
- 対応項目以外のバイトが変化しない。
- DBから読み、保存し直した結果がバイト単位で一致する。

### 同時利用テスト

- 公式アプリ相当の別SQLite接続から追加した行がLiteに表示される。
- Liteで追加した行を別SQLite接続から読み取れる。
- WAL使用中でも一覧を取得できる。
- 短い書き込みロック後に正常復帰する。
- 編集中に外部更新された行を上書きしない。

### 実アプリ受け入れテスト

1. 公式アプリで使い捨てプロファイルを作成する。
2. Liteを開き、設定なしで一覧に表示されることを確認する。
3. Liteで名前と設定を変更して保存する。
4. 公式アプリで同じ内容を読み込めることを確認する。
5. Liteで新規プロファイルを保存する。
6. 公式アプリに表示されることを確認する。
7. 公式アプリで変更した後、Liteへ戻るだけで更新が反映されることを確認する。
8. Liteで選択したプロファイルを実機へ適用し、D7 ACKを確認する。
9. 適用後に自動D6読み取りが実行されないことを確認する。

## 完了条件

- 起動直後にプロファイル一覧が表示される。
- コントローラー未接続でも既存プロファイルを編集できる。
- 公式アプリの既存プロファイルが設定なしで表示される。
- Liteで保存したプロファイルが公式アプリから利用できる。
- 公式アプリで保存した変更がLiteへ自動反映される。
- 保存と実機適用が明確に分離されている。
- 未知バイトとCRCを破壊しない。
- 同時起動中のDB競合でプロファイルを失わない。
- 起動時に危険なD6自動読み取りを行わない。
